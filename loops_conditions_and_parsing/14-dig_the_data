#!/usr/bin/env bash
# Groups visitors by IP and HTTP status code, then prints occurrences sorted descending.

awk '
# Strict Apache-ish format checks:
# - First field must look like IPv4
# - Status code must be exactly 3 digits
# - Must have at least 9 fields (so $9 exists)
$1 ~ /^[0-9]+\.[0]()
